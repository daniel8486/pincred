<%#= form_with(model: @about, local: true) do |form| %>
<%= form_with(model: [ :users_backoffice, @risk], local: true) do |form| %>
  <% if risk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(risk.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% risk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content%>
    <%= form.cktext_area :content %>
  </div>

  <div class="actions">
    <a class="waves-effect grey btn">  <%= form.submit %> </a>
  </div>
<% end %>
