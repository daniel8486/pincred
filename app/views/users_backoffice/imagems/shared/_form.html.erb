<%= form_with(model:[:users_backoffice, @imagem], local: true) do |form| %>
  <% if @imagem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(imagem.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% imagem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content%>
    <%= form.cktext_area :content %>
  </div>

<div class="field">
    <%= form.label :avatar%>
    <%= form.file_field :avatar %>
  </div>

<br> </br>
 
  <div class="actions">
    <a class="waves-effect grey btn">  <%= form.submit %> </a>
  </div>
<% end %>


